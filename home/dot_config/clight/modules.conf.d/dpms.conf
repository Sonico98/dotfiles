dpms:
{
	## Disabled because niri does not support DPMS protocols
    disabled = true;

    ## Timeouts on AC/on BATT.
    ## Set any of these to <= 0 to disable dpms
    ## in the corresponding AC state.
    timeouts = [ 480, 300 ];
};
